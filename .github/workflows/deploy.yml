name: Deploy Web Game and Release Desktop JAR

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-deploy-release:
    runs-on: ubuntu-latest

    steps:
      # Checkout
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Java
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Grant execute permission to gradlew
        run: chmod +x ./gradlew

      # Build Desktop JAR
      - name: Build Desktop JAR
        run: ./gradlew build

      # Build HTML5
      - name: Build HTML5 project
        run: ./gradlew html:dist

      # Upload JAR artifact (opcional, mas recomendado)
      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop-jar
          path: lwjgl3/build/libs/*.jar

      # Deploy HTML to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./html/build/dist

      # Create GitHub Release with JAR
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          name: Release v${{ github.run_number }}
          files: lwjgl3/build/libs/*.jar
